# ── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── Object Storage ───────────────────────────────────────────────────────────
minio>=7.2.0

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
psycopg2-binary==2.9.10
asyncpg==0.30.0
alembic==1.14.0

# ── Config & Auth ────────────────────────────────────────────────────────────
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
bcrypt==5.0.0

# ── AI & LLM (Phase 4-6) ─────────────────────────────────────────────────────
langchain==1.2.12
langchain-openai==1.1.11
langchain-google-genai==4.2.1
langgraph==1.1.1
langfuse==4.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
beautifulsoup4==4.14.3
requests==2.32.3

# ── PDF / Görüntü İşleme (Content Builder pipeline) ──────────────────────────
# pdfplumber: metin tabanlı PDF'ler için (hızlı, GPU yok)
# pdf2image:  PDF sayfalarını PIL görüntüsüne çevirir (GLM-OCR fallback için)
# Pillow:     görüntü okuma/dönüştürme (pdf2image ve GLM-OCR için gerekli)
# GLM-OCR:   Mert'in sunucusu üzerinden — pip install gerekmez, HTTP isteği yeterli
pdfplumber==0.11.4
pdf2image==1.17.0
Pillow==11.2.1

# ── Link / Video Extraction ───────────────────────────────────────────────────
# yt-dlp: YouTube ve diğer video sitelerinden ses indirir (Whisper transkript için)
yt-dlp>=2024.3.10
